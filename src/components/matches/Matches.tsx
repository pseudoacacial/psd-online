import { selectSelectors } from "../selectorsList/selectorsSlice"
import { selectDocument } from "../viewer/documentSlice"
import { useState } from "react"

export const Matches = () => {
  const [matches, setMatches] = useState([])

  return <div>Matches:</div>
}
